rules_version = '2';

/**
 * FIRESTORE SECURITY RULES - PRODUCTION AUDIT READY
 * 
 * DEPLOYMENT_FORCE_ID: 2024_05_26_FMOON_FULL_PRODUCTION_SYNC
 */

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}