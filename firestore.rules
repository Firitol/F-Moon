
rules_version = '2';

/**
 * FIRESTORE SECURITY RULES - PRODUCTION READY AUDIT
 * 
 * DEPLOYMENT_FORCE_ID: 2024_05_26_FMOON_PROD_READY
 */

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
