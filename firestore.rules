rules_version = '2';

/**
 * FIRESTORE SECURITY RULES - FULL PUBLIC ACCESS
 * Deployment Trigger: 2024-05-21T17:30:00Z
 */

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}